#include <vga/vga.h>

// Tests for the VGA subsystem.

// The VGA matrix that will be used by the function in the vga.c file.
#define MATRIX_SIZE (80 * 25)
static uint16_t fake_matrix[MATRIX_SIZE];

int main(int argc, char **argv) {
   // Allocate  
}
